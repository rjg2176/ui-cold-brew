$(document).ready(function(){

    // When the home or search page loads, do this stuff
    
    // Only add if homepage needs to have dynamic content 
    // (can delete this js file otherwise)
           

})